name: jest
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2
       - name: install dev
         run: |
           make install
       - name : run tests
         run: |
           make test
